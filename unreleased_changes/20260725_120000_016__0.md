* Feature : Support the modulo operator `%` in Cypher graph query arithmetic (e.g. `RETURN a.value % 10`), binding at the same precedence as `*` and `/`. Lowers to Stroom's existing `%` (modulo) expression operator.
