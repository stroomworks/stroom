* Feature : Add "Expand neighbours" to the Graph DB graph view - right-clicking a node expands its neighbours (all edge types, both directions) and merges them into the current graph, at the same temporal instant/window as the displayed query. Backed by a node-identity seek + all-type expand in the engine and a GET /graphDb/v1/{uuid}/expand endpoint.


```sh
# Issue number: 0
# ONLY the top line will be included as a change entry in the CHANGELOG.
```
