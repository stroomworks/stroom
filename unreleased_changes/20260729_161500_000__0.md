* Bug : Correct Javadoc contracts that referenced methods which do not exist and misstated join-key derivation: `BuildSideLookup`'s two-phase lifecycle rule was written entirely in terms of a `get` method the interface has never had (it is `forEachMatch`), and its key semantics claimed each component is the raw `Val#toString` when `JoinExecutor#keyOf` canonicalises numeric components — an implementor following it would have built a store whose numeric keys never matched the probe's.


```sh
# ONLY the top line will be included as a change entry in the CHANGELOG.
# The entry should be in GitHub flavour markdown and should be written on a SINGLE
# line with no hard breaks. You can have multiple change files for a single GitHub issue.
# The  entry should be written in the imperative mood, i.e. 'Fix nasty bug' rather than
# 'Fixed nasty bug'.
```
