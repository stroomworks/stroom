* Bug : Stop the query optimiser's advisory join cost model nominating a build side it cannot justify: a side with no cost signal was costed at zero rows and so always chosen as the side to hold in memory, and the join type was not passed in at all, so a `left join`'s preserved side could be nominated. `EXPLAIN` now reports that no build side was chosen when a side has no cost signal.


```sh
# ONLY the top line will be included as a change entry in the CHANGELOG.
# The entry should be in GitHub flavour markdown and should be written on a SINGLE
# line with no hard breaks. You can have multiple change files for a single GitHub issue.
# The  entry should be written in the imperative mood, i.e. 'Fix nasty bug' rather than
# 'Fixed nasty bug'.
```
