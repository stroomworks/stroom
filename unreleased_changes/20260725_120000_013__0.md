* Feature : Add date/time functions to Cypher `RETURN` — `formatDate`/`parseDate`, `formatDuration`/`parseDuration`, `now()`, `isWeekend`, and the `floor`/`round`/`ceiling` time-bucketing families (`floorDay`, `roundHour`, `ceilingMonth`, …) — wired to Stroom's existing functions under their Stroom names.
