* Feature : Add general maths functions `abs`, `sqrt`, `sign`, `exp` and `log` (natural log) to the expression language, usable in dashboards, StroomQL and Cypher graph queries (bare Cypher-standard names, and also as `stroom.abs(...)` etc.).
