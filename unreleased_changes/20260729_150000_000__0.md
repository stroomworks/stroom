* Feature : Document the per-query cost of `stroom.query.optimiser.mode: SHADOW` — a second compile, two whole-request JSON serialisations and a live meta-store aggregation, all synchronous — which was previously described only as "zero risk", and record the options for making a shadow soak cheap enough to leave on.


```sh
# ONLY the top line will be included as a change entry in the CHANGELOG.
# The entry should be in GitHub flavour markdown and should be written on a SINGLE
# line with no hard breaks. You can have multiple change files for a single GitHub issue.
# The  entry should be written in the imperative mood, i.e. 'Fix nasty bug' rather than
# 'Fixed nasty bug'.
```
