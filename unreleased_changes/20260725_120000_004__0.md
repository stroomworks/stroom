* Feature : Support `count(DISTINCT a.property)` in Cypher `RETURN`, counting distinct non-null values per group. `DISTINCT` on other aggregates, `count(DISTINCT *)`, and `count(DISTINCT <variable>)` are rejected with a clear compile error.
