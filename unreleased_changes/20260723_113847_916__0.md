* Feature : Add graph discovery to the Graph DB Data tab: a "Discover" panel listing the graph's node labels, relationship types, property keys and example nodes as clickable starter queries. Backed by a schema-discovery REST endpoint and a label-scoped `MATCH (n:Label) RETURN GRAPH` preview.


```sh
# ********************************************************************************
# Issue number: 0
# Issue title:
# Issue tags:
# Issue link:
# ********************************************************************************

# ONLY the top line will be included as a change entry in the CHANGELOG.
```
