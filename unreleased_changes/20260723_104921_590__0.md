* Feature : Support an unanchored `MATCH (n) RETURN GRAPH` whole-graph preview in the Graph DB engine (iterates the node and edge stores up to a ~100-node cap), and make it the Graph DB Data tab's default query so opening a graph shows its data and shape.


```sh
# ********************************************************************************
# Issue number: 0
# Issue title:
# Issue tags:
# Issue link:
# ********************************************************************************

# ONLY the top line will be included as a change entry in the CHANGELOG.
```
