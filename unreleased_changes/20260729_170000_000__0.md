* Build : Restore Javadoc doclint validation, as a per-module ratchet, for the six query and Graph DB modules whose Javadoc is now clean, so a broken `{@link}`, a `@param` naming a parameter that does not exist, or malformed HTML fails the build again rather than being silently ignored, and fix the twelve such defects that enabling it surfaced.


```sh
# ONLY the top line will be included as a change entry in the CHANGELOG.
# The entry should be in GitHub flavour markdown and should be written on a SINGLE
# line with no hard breaks. You can have multiple change files for a single GitHub issue.
# The  entry should be written in the imperative mood, i.e. 'Fix nasty bug' rather than
# 'Fixed nasty bug'.
```
