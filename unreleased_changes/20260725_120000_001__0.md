* Bug : Fix `IN` list membership on text fields: `ExpressionPredicateFactory`'s `StringIn` split candidate values on a single space while every producer joins them with a comma, so a comma-joined text `IN` never matched. It now splits on the shared comma delimiter and trims each value, consistent with the numeric/date `IN` paths.
