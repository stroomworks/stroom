* Feature : Add a Cytoscape.js graph view to the Graph DB Data tab, with a Table/Graph toggle that renders a `RETURN GRAPH` result as an interactive graph (foundation: sandboxed-iframe host, row-to-elements adapter, view toggle).


```sh
# ********************************************************************************
# Issue number: 0
# Issue title:
# Issue tags:
# Issue link:
# ********************************************************************************

# ONLY the top line will be included as a change entry in the CHANGELOG.
```
