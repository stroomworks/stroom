* Feature : Support `collect(a.property)` and `collect(DISTINCT a.property)` in Cypher `RETURN`, gathering a group's non-null values. In this version the result is a comma-joined string (a true list-valued result is deferred). `collect(*)` and `collect(<variable>)` are rejected with a clear compile error.
