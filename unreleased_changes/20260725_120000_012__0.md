* Feature : Add Cypher graph-identity functions `id(v)` (a matched node's external id) and `type(r)` (a traversed relationship's type) to `RETURN`. `labels(v)`/`keys(v)`/`properties(v)` return a list/map and are rejected until a list-valued type exists. Identity is populated on fixed-length patterns.
