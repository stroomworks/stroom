* Feature : Record that cost-driven join strategy selection is blocked on two `JoinCostModel.chooseAlgorithm` defects — a no-signal side being costed at zero rows and so chosen as the build side, and the join type not being passed in — and plan them, plus documenting the executor's measured-size selection as intended behaviour, as Phase 7 of the query-optimiser implementation plan.


```sh
# ONLY the top line will be included as a change entry in the CHANGELOG.
# The entry should be in GitHub flavour markdown and should be written on a SINGLE
# line with no hard breaks. You can have multiple change files for a single GitHub issue.
# The  entry should be written in the imperative mood, i.e. 'Fix nasty bug' rather than
# 'Fixed nasty bug'.
```
