* Feature : Support `OPTIONAL MATCH` (left-outer) in Cypher, e.g. `MATCH (p:Person {id:'p1'}) OPTIONAL MATCH (p)-[:PARTY_TO]->(c:Crime) RETURN p.id, count(c)` - anchors with no optional match still appear, with the optional variable's properties null and `count()` over it 0. v1 scope: exactly one optional hop extending the preceding MATCH's final variable, no WHERE/temporal clause on the OPTIONAL MATCH, and not combined with DIFF/RETURN GRAPH; other shapes are rejected with a clear compile error.
