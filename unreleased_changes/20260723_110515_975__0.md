* Feature : Support an optional `LIMIT n` on Cypher `RETURN GRAPH`, bounding the result to n nodes plus the edges between them (rejected on a `DIFF ... RETURN GRAPH`). The Graph DB Data tab's default query now reads `MATCH (n) RETURN GRAPH LIMIT 100`.


```sh
# ********************************************************************************
# Issue number: 0
# Issue title:
# Issue tags:
# Issue link:
# ********************************************************************************

# ONLY the top line will be included as a change entry in the CHANGELOG.
```
