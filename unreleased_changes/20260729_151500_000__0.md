* Bug : Correct the recorded cause of the query editor's pre-run slow-query warning never appearing: it is a client-side defect (`estimatedDurationMs` is set only on a leaf `Scan` node while `QueryModel` reads the plan root), not a consequence of the stubbed cost adapters, so landing real adapters would not make it fire.


```sh
# ONLY the top line will be included as a change entry in the CHANGELOG.
# The entry should be in GitHub flavour markdown and should be written on a SINGLE
# line with no hard breaks. You can have multiple change files for a single GitHub issue.
# The  entry should be written in the imperative mood, i.e. 'Fix nasty bug' rather than
# 'Fixed nasty bug'.
```
